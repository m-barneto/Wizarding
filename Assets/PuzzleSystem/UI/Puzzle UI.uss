:root {
    --bg: rgb(127, 106, 86);
    --panel: rgb(90, 70, 51);
    --btn: rgb(63, 47, 34);
    --btn-border: rgb(154, 131, 108);
    --accent: rgb(220, 211, 200);
    --tile-size: 56px;
    --tile-gap: 6px;
    --corner: 6px;
}

.root {
    width: 340px;
    height: 720px;
    background-color: var(--bg);
    padding: 10px;
    border-radius: 6px;
    text-shadow: 0 2px 6px rgba(0, 0, 0, 0.6);
    flex-direction: column;
}

.header {
    display: flex;
    flex-direction: column;
    margin-top: 3px;
    margin-bottom: 8px;
}

.topButton {
    background-color: var(--btn);
    color: var(--accent);
    height: 30px;
    -unity-font-style: Bold;
    border-radius: var(--corner);
    border-width: 2px;
    border-color: var(--btn-border);
    align-self: stretch;
    -unity-text-align: middle-center;
}

.label {
    -unity-text-align: middle-center;
    color: var(--accent);
    margin-top: 4px;
}

.sliderRow {
    display: flex;
    flex-direction: row;
    align-items: center;
    margin: 3px;
    align-content: center;
    align-self: stretch;
    justify-content: flex-start;
    top: 5px;
}

.radiusValue {
    min-width: 100px;
    width: auto;
    -unity-text-align: middle-center;
    color: var(--accent);
}

.iconGrid {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    margin: 3px;
    align-content: flex-start;
    padding: 6px;
    background-color: var(--btn);
    border-radius: var(--corner);
}

.iconTile {
    width: var(--tile-size);
    height: var(--tile-size);
    border-radius: 6px;
    border-width: 2px;
    border-color: rgb(107, 84, 64);
    background-color: rgb(59, 47, 40);
    align-items: center;
    justify-content: center;
    padding: 6px;
}

.iconTile-selected {
    border-color: #e5d2a8;
    background-color: #4b3b2f;
    text-shadow: 0 0 6px rgba(255,255,200,0.5);
}

.iconTile > .iconImage {
    width: 80%;
    height: 80%;
    -unity-background-scale-mode: scale-to-fit;
    background-color: transparent;
}

.iconTile:hover {
    border-color: rgb(203, 183, 157);
}

.tooltip {
    position: absolute;
    display: none;
    padding: 6px 8px;
    border-radius: 4px;
    border-width: 1px;
    border-color: #3b2f28;
    background-color: rgba(20, 20, 20, 0.9);
    color: #f5f1e6;
    -unity-font-style: Bold;
    font-size: 12px;
    max-width: 260px;
    -unity-text-align: middle-left;
}
